class Building {
  constructor(name, address) {
    this._name = name;
    this._address = address;
    this._floors = [];
  }
  constructFloor(num) {
    const Floor = new Floor(num);
    this._floors[number] = floor;
    return floor;
  }
}
